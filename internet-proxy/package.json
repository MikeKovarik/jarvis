{
  "name": "jarvis-proxy",
  "version": "0.0.1",
  "private": true,
  "esm": {
    "cjs": true
  },
  "scripts": {
    "start": "pm2 stop jarvis-proxy-tunnel && pm2 flush && pm2 start proxytunnel.cjs --name jarvis-proxy-tunnel && pm2 logs jarvis-proxy-tunnel"
  },
  "dependencies": {
    "lan-tunnel": "^2.0.0"
  },
  "devDependencies": {
    "esm": "^3.2.25"
  }
}
